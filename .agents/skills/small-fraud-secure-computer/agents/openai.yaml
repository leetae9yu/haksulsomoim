interface:
  display_name: "소액사기 보안 Computer Use"
  short_description: "로컬 마스킹 후 한국 전자소송 절차를 보조합니다."
  default_prompt: "확인된 사건 사실과 현재 포털 화면을 기준으로 다음 한 단계만 안전하게 진행하세요."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: mcp
      value: haksulsomoim-secure-computer
      description: "Locally redacts observations and executes only digest-bound allowlisted browser actions."
