# Observation lanes

Store one append-only machine ledger per `LANE` in this directory. Each ledger follows [[SCHEMA]] and has exactly one fenced `jsonl` block containing only `observation` records for its lane.
